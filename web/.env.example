# Mapbox public access token (pk....) — required for the map to render.
# This is the only configuration the frontend needs; routing is computed
# client-side, so there is no backend URL to set.
VITE_MAPBOX_TOKEN=
